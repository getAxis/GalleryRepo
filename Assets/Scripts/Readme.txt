//DataHolder класс существующий для одной единственной цели - контейнер данных, 
в нашем случае запоминает номер картинки которую выбрал пользователь и передает в сцену ImageScene.

//ImageLoader класс используется в сцене Gallery, назначается на GameManager, необходим для Создания заданного количества картинок
и последующего назначения на каждую изображения из сервера.

//ProgressBar скрипт реализующий логику поведения ProgressBar в сцене LoadScene.

//Loading скрипт который реализует загрузку сцены "Gallery" перед ее открытием.

//SingleImageLoad скрипт, который загружает с сервера изображение, которое было выбрано в сцене "Gallery".

//SwipeDetector - скрипт, отвечающий за возврат на предыдущую сцену или главное меню с помощью свайпа вправо, его я так и не успел 
довести до ума, не используется.

//TapToImage класс, который сохраняет значение имени выбранного изображения и передает его в DataHolders

//UIcontroller класс, содержащий функции для разначения их к соответствующий кнопкам, таким как Return





